---
- name: Generate template to create EC2 instance(s)
  template:
    src: templates/instances.yml.j2
    dest: "{{ekara_temp_dir}}/instances-{{ek_config.environment.id}}.yml"

- name: Provision EC2 instance(s)
  include_tasks: "{{ekara_temp_dir}}/instances-{{ek_config.environment.id}}.yml"
